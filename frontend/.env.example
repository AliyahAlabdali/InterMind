VITE_API_BASE_URL=http://127.0.0.1:8000

# Milestone 4 recruiter access token (see app.api.auth on the backend) - must match the
# backend's RECRUITER_ACCESS_TOKEN (or its dev default) exactly. Not production auth; see that
# module's docstring.
VITE_RECRUITER_ACCESS_TOKEN=dev-recruiter-milestone-token-change-me
